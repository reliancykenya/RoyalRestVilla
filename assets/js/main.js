document.addEventListener('DOMContentLoaded',()=>{const btn=document.querySelector('.rrv-menu-toggle');const nav=document.querySelector('.rrv-nav');if(btn&&nav){btn.addEventListener('click',()=>{const open=nav.classList.toggle('is-open');btn.setAttribute('aria-expanded',open?'true':'false');});}const checkin=document.querySelector('input[name="checkin"],input[name="rrv_checkin"]');const checkout=document.querySelector('input[name="checkout"],input[name="rrv_checkout"]');if(checkin&&checkout){checkin.addEventListener('change',()=>{if(!checkin.value)return;const d=new Date(checkin.value+'T12:00:00');d.setDate(d.getDate()+1);checkout.min=d.toISOString().slice(0,10);if(checkout.value&&checkout.value<=checkin.value)checkout.value='';});}});